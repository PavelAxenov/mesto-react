export const enum ButtonSize {
	Sm = 'sm',
	Md = 'md',
	Lg = 'lg',
}

export const enum ButtonVariant {
	Default = 'default',
	Border = 'border',
}

export const enum ButtonType {
	Submit = 'submit',
	Reset = 'reset',
	Default = 'button',
}